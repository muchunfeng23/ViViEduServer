<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- <mapper namespace="cn.no7player.mapper.UserMapper"> -->
<mapper namespace="com.mcf.mybatis.mapper.StoreMapper">
 	<select id="getStoreById" parameterType="string" resultType="int">
 		select count(1) from ixue_store where store_uuid = #{storeId}
 	</select>
	
</mapper>